[pytest]
testpaths = tests
addopts = -ra --strict-markers --tb=short --durations=10
markers =
    unit: tests rapides sans service externe
    integration: tests avec services Docker
    api: tests FastAPI
    docker: tests Docker
    mlflow: tests MLflow
    postgres: tests PostgreSQL
    minio: tests MinIO
